import React from "react";
import ReactDOM from "react-dom/client";
import LighthousePointAdvisorsSite from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LighthousePointAdvisorsSite />
  </React.StrictMode>
);
